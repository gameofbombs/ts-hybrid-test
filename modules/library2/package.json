{
  "name": "@test/library2",
  "version": "1.0.0",

  "main": "lib/library2.js",
  "module": "lib/library2.es.js",

  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build:dev": "rollup -c",
    "build": "rollup -cp",
    "watch": "rollup -cw",
    "postversion": "npm run build",
    "test": "exit 0"
  },
  "files": [
    "lib"
  ],
  "devDependencies": {
    "@internal/builder": "^1.0.0",
    "@test/library1": "^1.0.0",
    "rollup": "^0.50.0"
  }
}
